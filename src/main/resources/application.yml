server:
  port: 8080
  servlet:
    session:
      tracking-modes: cookie

spring:
  application:
    name: Progressor Application


  datasource:
    url: jdbc:mysql://localhost:3316/progress
    username: kty
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  thymeleaf:
    cache: false
#logging.level:
#  org.hibernate.SQL: debug
#  org.hibernate.type : trace

